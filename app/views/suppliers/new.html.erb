<%= render TurbomodalComponent.new(title: "New Supplier") do %>
  <div class="mx-auto w-full">
    <div class="pb-5 border-b border-gray-200">
      <h1 class="font-bold text-4xl">New Supplier</h1>
    </div>

    <%= render "form", supplier: @supplier %>
  </div>
<% end %>