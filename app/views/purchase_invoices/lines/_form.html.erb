<%= form_with(model: purchase_invoice_line, class: "contents") do |form| %>
  <% if purchase_invoice_line.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(purchase_invoice_line.errors.count, "error") %> prohibited this purchase_invoice_line from being saved:</h2>

      <ul>
        <% purchase_invoice_line.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :company_id %><br>
    <%= form.collection_select :company_id, Company.all, :id, :to_s, prompt: true  %>
  </div>

  <div class="my-5">
    <%= form.label :account_number_id %><br>
    <%= form.collection_select :account_number, Account.all, :number, :to_s, prompt: true  %>
  </div>

  <div class="my-5">
    <%= form.label :account_number_id %><br>
    <%= form.collection_select :account_number, Account.all, :number, :to_s, prompt: true  %>
  </div>

  <div class="my-5">
    <%= form.label :amount %>
    <%= form.text_field :amount, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="my-5">
    <%= form.label :tax_rate %>
    <%= form.text_field :tax_rate, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="my-5">
    <%= form.label :tax_account_number_id %><br>
    <%= form.collection_select :tax_account_number, Account.all, :number, :to_s, prompt: true  %>
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>